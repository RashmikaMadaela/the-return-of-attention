import { Navigation } from '@/components/Navigation'
import PAHMMatrix from '@/components/PAHMMatrix'

export default function LandingPage() {
      title: 'Physical Stillness',
      description: 'Master the art of sitting still',
      duration: '1-2 weeks',
      isUnlocked: true,
      isCompleted: true
    },
    {
      id: 2,
      title: 'Thought Patterns',
      description: 'Observe your mental patterns',
      duration: '2-3 weeks',
      isUnlocked: true,
      isCompleted: false,
      isCurrent: true
    },
    {
      id: 3,
      title: 'Dot Tracking',
      description: 'Develop focused attention',
      duration: '2-3 weeks',
      isUnlocked: false,
      isCompleted: false
    }
  ]

  return (
    <main className="min-h-screen bg-white">
      {/* Hero Section - Exact Figma Match */}
      <section className="relative min-h-screen flex items-center justify-center" style={{ background: 'linear-gradient(135deg, #3b82f6 0%, #60a5fa 40%, #93c5fd 80%, #dbeafe 100%)' }}>
        
        {/* Decorative background elements */}
        <div className="absolute inset-0 overflow-hidden">
          <div className="absolute top-20 left-10 w-32 h-32 bg-blue-300/20 rounded-full blur-xl"></div>
          <div className="absolute top-40 right-20 w-24 h-24 bg-blue-200/30 rounded-full blur-lg"></div>
          <div className="absolute bottom-32 left-1/4 w-40 h-40 bg-blue-400/15 rounded-full blur-2xl"></div>
          <div className="absolute bottom-20 right-10 w-28 h-28 bg-blue-300/25 rounded-full blur-xl"></div>
        </div>

        <div className="container relative z-10 px-4">
          <div className="max-w-2xl mx-auto text-center">
            
            {/* PAHM Matrix - Centered and Prominent */}
            <div className="mb-12">
              <PAHMMatrix 
                isInteractive={false}
                size="lg"
                className="mx-auto"
              />
            </div>
            
            {/* Main Content */}
            <div className="space-y-6">
              <h1 className="text-4xl md:text-5xl lg:text-6xl font-bold text-white leading-tight">
                "You are not your thoughts"
              </h1>
              
              <h2 className="text-lg md:text-xl font-medium text-white/90">
                Practices for the Happiness that Stays
              </h2>
              
              <p className="text-base md:text-lg text-white/80 max-w-lg mx-auto leading-relaxed">
                A simple, practical guide to happiness that actually stays
              </p>
              
              {/* CTA Button - Exact Figma Style */}
              <div className="pt-8">
                <a href="/register" 
                   className="inline-block px-10 py-4 bg-blue-600 hover:bg-blue-700 text-white font-semibold rounded-full shadow-2xl transform hover:scale-105 transition-all duration-200 text-lg">
                  Practice Today!
                </a>
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* Problem/Solution Section - Figma Match */}
      <section className="py-16 bg-gray-50">
        <div className="container max-w-6xl mx-auto px-4">
          <div className="grid lg:grid-cols-2 gap-12">
            
            {/* Sound Familiar? */}
            <div className="space-y-6">
              <h2 className="text-2xl font-bold text-gray-900 mb-8">
                Sound Familiar?
              </h2>
              <div className="space-y-4">
                {[
                  "Mind won't quiet down",
                  "Constant worry and stress", 
                  "Feeling trapped in thoughts",
                  "Nothing feels quite right",
                  "Always seeking, never satisfied"
                ].map((text, index) => (
                  <div key={index} className="bg-teal-100 text-gray-800 px-6 py-4 rounded-lg border border-teal-200">
                    <p className="font-medium">{text}</p>
                  </div>
                ))}
              </div>
            </div>

            {/* What If There's a Way Out? */}
            <div className="bg-teal-200 p-8 rounded-2xl relative">
              
              {/* Meditation Icon Circle */}
              <div className="absolute top-8 right-8 w-16 h-16 bg-yellow-300 rounded-full flex items-center justify-center">
                <span className="text-2xl">🧘</span>
              </div>

              <h2 className="text-2xl font-bold text-gray-900 mb-6">
                What If There's a Way Out?
              </h2>
              <div className="space-y-4 text-gray-800">
                <div className="mb-6">
                  <h3 className="text-lg font-semibold mb-2">The Universal Solution</h3>
                  <p className="text-sm leading-relaxed">Unlike your body, your mind is trainable. Thousands have found lasting peace through this simple practice.</p>
                </div>
                
                <div>
                  <h3 className="text-lg font-semibold mb-2">6 Stages</h3>
                  <p className="text-sm">From chaos to clarity</p>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* Journey Overview - Figma Style */}
      <section className="py-16" style={{ background: 'linear-gradient(135deg, #3b82f6 0%, #60a5fa 50%, #93c5fd 100%)' }}>
        <div className="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="text-center mb-12">
            <h2 className="text-3xl md:text-4xl font-bold text-white mb-8">Your Journey to Freedom</h2>
          </div>
          
          {/* 6 Stages Grid - Figma Style */}
          <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-6 mb-12">
            
            {/* Stage 1-2 */}
            <div className="bg-white rounded-xl p-6 shadow-lg">
              <div className="w-8 h-8 bg-blue-100 rounded-full flex items-center justify-center mb-4">
                <span className="text-blue-600 text-sm font-bold">1</span>
              </div>
              <h3 className="text-lg font-semibold text-gray-900 mb-2">Stage 1-2</h3>
              <p className="text-sm text-gray-600 mb-4">Physical stillness & thought observation</p>
            </div>

            {/* Stage 3-4 */}
            <div className="bg-white rounded-xl p-6 shadow-lg">
              <div className="w-8 h-8 bg-blue-100 rounded-full flex items-center justify-center mb-4">
                <span className="text-blue-600 text-sm font-bold">3</span>
              </div>
              <h3 className="text-lg font-semibold text-gray-900 mb-2">Stage 3-4</h3>
              <p className="text-sm text-gray-600 mb-4">Recognizing mental patterns</p>
            </div>

            {/* Stage 5-6 */}
            <div className="bg-white rounded-xl p-6 shadow-lg">
              <div className="w-8 h-8 bg-blue-100 rounded-full flex items-center justify-center mb-4">
                <span className="text-blue-600 text-sm font-bold">5</span>
              </div>
              <h3 className="text-lg font-semibold text-gray-900 mb-2">Stage 5-6</h3>
              <p className="text-sm text-gray-600 mb-4">Discovering lasting happiness</p>
            </div>

            {/* No Beliefs */}
            <div className="bg-white rounded-xl p-6 shadow-lg">
              <div className="w-8 h-8 bg-gray-100 rounded-full flex items-center justify-center mb-4">
                <span className="text-gray-600 text-sm font-bold">✓</span>
              </div>
              <h3 className="text-lg font-semibold text-gray-900 mb-2">No Beliefs</h3>
              <p className="text-sm text-gray-600 mb-4">Just practical experience</p>
            </div>

            {/* No Special Skills */}
            <div className="bg-white rounded-xl p-6 shadow-lg">
              <div className="w-8 h-8 bg-gray-100 rounded-full flex items-center justify-center mb-4">
                <span className="text-gray-600 text-sm font-bold">✓</span>
              </div>
              <h3 className="text-lg font-semibold text-gray-900 mb-2">No Special Skills</h3>
              <p className="text-sm text-gray-600 mb-4">Anyone can do this</p>
            </div>

            {/* Happiness That Stays */}
            <div className="bg-white rounded-xl p-6 shadow-lg">
              <div className="w-8 h-8 bg-green-100 rounded-full flex items-center justify-center mb-4">
                <span className="text-green-600 text-sm font-bold">✨</span>
              </div>
              <h3 className="text-lg font-semibold text-gray-900 mb-2">Happiness That Stays</h3>
              <p className="text-sm text-gray-600 mb-4">Not dependent on circumstances</p>
            </div>
          </div>

          {/* The Simple Truth */}
          <div className="bg-white/10 backdrop-blur-sm rounded-2xl p-8 text-center">
            <h3 className="text-2xl font-bold text-white mb-6">The Simple Truth</h3>
            <p className="text-white/90 text-lg leading-relaxed mb-6">
              Most approaches to happiness involve adding something new. This approach is different. It involves removing the obstacles to the peace that is already your natural state.
            </p>
            <p className="text-white/80 text-sm">
              ↩️ Notice when attention wanders ↩️ Gently return to the present 🔄 Repeat until it becomes natural
            </p>
          </div>
        </div>
      </section>
                <div className="flex items-center gap-2 px-4 py-2 rounded-full bg-gray-800">
                  <span className="text-lg">👁️</span>
                  <span className="text-sm">Notice</span>
                </div>
                <span className="text-2xl">↩️</span>
                <div className="flex items-center gap-2 px-4 py-2 rounded-full bg-gray-800">
                  <span className="text-lg">🎯</span>
                  <span className="text-sm">Return</span>
                </div>
                <span className="text-2xl">🔄</span>
                <div className="flex items-center gap-2 px-4 py-2 rounded-full bg-gray-800">
                  <span className="text-lg">✨</span>
                  <span className="text-sm">Natural</span>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* Ready to Begin */}
      <section className="py-20" style={{ background: 'linear-gradient(135deg, var(--blue-50) 0%, var(--teal-50) 50%, var(--purple-50) 100%)' }}>
        <div className="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
          <div className="w-32 h-32 rounded-full flex items-center justify-center mx-auto mb-8 animate-pulse-glow"
               style={{ background: 'linear-gradient(135deg, var(--blue-200) 0%, var(--teal-200) 50%, var(--purple-200) 100%)' }}>
            <span className="text-6xl">✨</span>
          </div>
          
          <h2 className="text-h1 mb-6">Ready to Begin?</h2>
          <p className="text-body-large text-muted mb-12 max-w-2xl mx-auto">
            Join thousands who have discovered that lasting happiness isn't something to achieve— 
            it's something to <em className="font-semibold" style={{ color: 'var(--teal-600)' }}>recognize</em>.
          </p>
          
          <div className="space-y-4 sm:space-y-0 sm:space-x-6 sm:flex sm:justify-center mb-8">
            <a
              href="/register"
              className="btn btn-primary btn-lg block sm:inline-block"
            >
              🚀 Start Your Journey
            </a>
            <a
              href="/questionnaire"
              className="btn btn-secondary btn-lg block sm:inline-block"
            >
              📝 Take Assessment
            </a>
          </div>
          
          <div className="flex items-center justify-center gap-8 text-sm text-muted">
            <div className="flex items-center gap-2">
              <span className="text-green-500">✓</span>
              <span>No subscription required</span>
            </div>
            <div className="flex items-center gap-2">
              <span className="text-green-500">✓</span>
              <span>Complete guide included</span>
            </div>
            <div className="flex items-center gap-2">
              <span className="text-green-500">✓</span>
              <span>Start immediately</span>
            </div>
          </div>
        </div>
      </section>

      {/* Footer */}
      <footer className="bg-slate-900 text-white py-12">
        <div className="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
          <blockquote className="text-xl mb-4 italic">
            "This book offers a practice, not a philosophy. It is about returning— bringing attention back to what is already here."
          </blockquote>
          <p className="text-slate-400 mb-2">— A.C. Amarasinghe</p>
          <p className="text-slate-400 mb-4">The Return of Attention</p>
          <p className="text-slate-500 text-sm">© All Rights Reserved. 2025</p>
        </div>
      </footer>
    </main>
  )
}